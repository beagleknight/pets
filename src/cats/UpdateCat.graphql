mutation UpdateCat($id: ID!, $input: UpdateCatInput!) {
  catsMutations {
    update(id: $id, input: $input) {
      cat {
        id
        name
      }
      errors {
        message
      }
    }
  }
}
